[Unit]
Description=Smart Home Monitor Server
After=network.target multi-user.target

[Service]
Type=forking
ExecStart=cd @PREFIX@-server && node iotserver.js
ExecStop=/bin/kill -15 $MAINPID
Restart=always

[Install]
WantedBy=multi-user.target
